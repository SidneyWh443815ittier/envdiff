// Package linter provides rule-based linting for .env files,
// detecting issues such as duplicate keys, empty values,
// unquoted values containing spaces, and non-standard key formatting.
package linter
