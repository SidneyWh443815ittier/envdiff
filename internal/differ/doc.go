// Package differ orchestrates the full diff pipeline: loading env files,
// comparing key-value pairs, and applying filters to produce a set of
// per-file diff results ready for reporting.
package differ
